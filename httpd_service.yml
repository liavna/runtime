apiVersion: v1
kind: Service
metadata:
  name: httpd-svc
  namespace: user2
  labels:
    hpecp.hpe.com/hpecp-internal-gateway: "true"
spec:
  ports:
    - name: http
      port: 80
      targetPort: 9000
  selector:
    app: httpd
  type: NodePort
